window.addEventListener('resize', () => {
    // Recalculate carousel and slide dimensions if needed
    const carousel = document.getElementById('carousel-example-2');
    carousel.style.height = '100vh';
    carousel.style.width = '100vw';
});


//position in page caption

